import 'package:flutter/material.dart';

class CustomWorkoutsPage extends StatelessWidget {
  const CustomWorkoutsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Custom Workouts")));
  }
}